# Language pages and /prequel are real files served directly by Cloudflare Pages
# (clean URLs are automatic: /prequel <- prequel.html, /fr/ <- fr/index.html).
# No rewrites needed — a /prequel -> /prequel.html rule here causes a redirect loop
# because Pages also auto-redirects /prequel.html -> /prequel.
